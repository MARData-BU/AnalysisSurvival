% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyze_survival.R
\name{analyze_survival}
\alias{analyze_survival}
\title{Run Automated Survival Analysis, KM Plotting, and Cox Modeling}
\usage{
analyze_survival(
  data,
  outcome = NA,
  time_var,
  event_var,
  test_var,
  covariates = c(),
  interaction_var = NULL,
  ref_level = NULL,
  analysis_name = NA,
  filename = "KM.png",
  conf_int = 0.95,
  custom_colors = default_colors,
  custom_linetypes = 1,
  break_time_by = NULL,
  plot = TRUE
)
}
\arguments{
\item{data}{A data.frame containing clinical/survival data.}

\item{outcome}{Character string describing the outcome (e.g., "Overall Survival").}

\item{time_var}{Character string specifying column name for time.}

\item{event_var}{Character string specifying column name for event/status (0/1 or FALSE/TRUE).}

\item{test_var}{Character string specifying primary grouping variable to test.}

\item{covariates}{Optional character vector of covariate column names for multivariable adjustment.}

\item{interaction_var}{Optional character vector for interaction term testing.}

\item{ref_level}{Optional character string specifying reference level for \code{test_var}.}

\item{analysis_name}{Title label for the analysis and plot.}

\item{filename}{File path/name for saving the KM plot PNG (default "KM.png").}

\item{conf_int}{Numeric confidence level (default 0.95).}

\item{custom_colors}{Vector of hex color codes or color names for plot strata.}

\item{custom_linetypes}{Vector of linetypes for plot strata.}

\item{break_time_by}{Step size for x-axis time breaks. Automatically calculated if NULL.}

\item{plot}{Logical; whether to draw and export PNG plot (default TRUE).}
}
\value{
A data.frame containing sample sizes, median survival with CIs,
  hazard ratios (HR/aHR) with CIs, log-rank p-values, proportional hazards test
  p-values, and interaction p-values.
}
\description{
Generates Kaplan-Meier survival curves, fits Cox proportional hazards
models (univariate or multivariable), checks proportional hazards
assumptions via Schoenfeld residuals, and outputs summary statistics.
}
\examples{
\dontrun{
# 1. Univariate model (strata > 1)
analyze_survival(
  data = data, 
  outcome = "PFS", 
  time_var = "pfs_time", 
  event_var = "pfs_event", 
  test_var = "Sex", 
  ref_level = "Male", 
  analysis_name = "Sex", 
  filename = file.path(resultsDir, "Male_vs_Female.png"), 
  covariates = NULL, 
  interaction_var = NULL,
  conf_int = 0.90
)

# 2. Multivariable model (without interaction)
analyze_survival(
  data = data, 
  outcome = "PFS", 
  time_var = "pfs_time", 
  event_var = "pfs_event", 
  test_var = "Sex", 
  ref_level = "Male", 
  analysis_name = "Sex_multi_Age", 
  filename = file.path(resultsDir, "Male_vs_Female.png"), 
  covariates = "Age", 
  interaction_var = NULL,
  conf_int = 0.90
)

# 3. Multivariable model (with interaction)
analyze_survival(
  data = data, 
  outcome = "PFS", 
  time_var = "pfs_time", 
  event_var = "pfs_event", 
  test_var = "Sex", 
  ref_level = "Male", 
  analysis_name = "Sex_interact_Age", 
  filename = file.path(resultsDir, "Male_vs_Female.png"), 
  covariates = "Age", 
  interaction_var = "Age",
  conf_int = 0.90
)

# 4. Univariate model (strata == 1)
analyze_survival(
  data = data[which(data$Sex == "Female"), ], 
  outcome = "PFS", 
  time_var = "pfs_time", 
  event_var = "pfs_event", 
  test_var = "Sex", 
  analysis_name = "Sex", 
  filename = file.path(resultsDir, "Female_distribution.png"), 
  covariates = NULL, 
  interaction_var = NULL,
  conf_int = 0.90
)
}
}
